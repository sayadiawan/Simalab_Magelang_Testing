(function($) {
  'use strict';
  // Hover expand sidebar dinonaktifkan.
  // Mode sidebar-icon-only + submenu expand ditangani di template/admin/layout.blade.php
  // agar tidak bentrok dengan klik collapse Bootstrap.
})(jQuery);
